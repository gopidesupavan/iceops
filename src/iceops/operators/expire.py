from __future__ import annotations

from ..errors import NotYetImplemented


def expire(*args: object, **kwargs: object) -> None:
    """v0.2: snapshot expiration via PyIceberg, with dry-run reporting of freed bytes."""
    raise NotYetImplemented("expire", "v0.2")
