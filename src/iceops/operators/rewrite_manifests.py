from __future__ import annotations

from ..errors import NotYetImplemented


def rewrite_manifests(*args: object, **kwargs: object) -> None:
    """v0.2: manifest consolidation (metadata-only, pure Python)."""
    raise NotYetImplemented("rewrite-manifests", "v0.2")
